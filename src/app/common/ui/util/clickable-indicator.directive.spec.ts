import { ClickableIndicatorDirective } from './clickable-indicator.directive';

describe('ClickableIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickableIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
