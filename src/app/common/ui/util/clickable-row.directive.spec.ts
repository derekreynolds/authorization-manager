import { ClickableRowDirective } from './clickable-row.directive';

describe('ClickableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickableRowDirective();
    expect(directive).toBeTruthy();
  });
});
